---
 apiVersion: v1
 kind: Service
 metadata:
 name: pg-prometheuss-lb
 spec:
  type: LoadBalancer
  selector:
    app: pg-prometheuss
  ports:
    - name: db1-dbport
      protocol: TCP
      port: 5432
      targetPort: db1-dbport
  selector:
    app: pg-prometheuss
